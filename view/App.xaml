<Application x:Class="view.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converter="clr-namespace:view.Converter"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <converter:ColorConverter x:Key="ColorConverter" />
        <converter:DateConverter x:Key="DateConverter" />

        <DataTemplate x:Key="MessagesTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Contenu}" Foreground="Red"/>
                <TextBlock Text=" : " Foreground="Black"/>
                <TextBlock Text="{Binding Emetteur}" Foreground="Brown" />
                <TextBlock Text=", le " Foreground="BlanchedAlmond"/>
                <TextBlock Text="{Binding Date, Converter={StaticResource DateConverter}}" Foreground="Blue" />
                <Button Content="Edit Message" Command="{Binding DataContext.EditThisMessage, RelativeSource={RelativeSource AncestorType={x:Type ListBox}},Mode=OneWay}" CommandParameter="{Binding .}"/>
                <Button Content="Delete Message" Command="{Binding DataContext.DeleteThisMessage, RelativeSource={RelativeSource AncestorType={x:Type ListBox}},Mode=OneWay}" CommandParameter="{Binding .}"/>
            </StackPanel>
        </DataTemplate>

    </Application.Resources>
    
    
</Application>
