<UserControl x:Class="view.UserControls.UserControlRandom"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="{Binding RandomNumber}" Foreground="{Binding RandomNumber, Converter={StaticResource ColorConverter}}"/>
        <Button Content="ramdom" Command="{Binding ChangeNumber}"/>
    </StackPanel>
</UserControl>
